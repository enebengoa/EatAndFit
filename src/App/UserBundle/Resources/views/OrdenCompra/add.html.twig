{{ form_start(formOrdenCompra, {'class': 'ajaxForm',  'attr': {'class': 'ajaxForm'}}) }}

{% include "UserBundle:OrdenCompra:form.html.twig"  with {'formOrdenCompra': formOrdenCompra} %}

{{ form_end(formOrdenCompra) }}
