{{ form_start(formTipo, {'class': 'ajaxForm', 'action': path('App_admin_addTipoReceta'), 'attr': {'class': 'ajaxForm'}}) }}

{% include "AdminBundle:TipoReceta:form.html.twig"  with {'formTipo': formTipo} %}

{{ form_end(formTipo) }}