{% block body %}
	<form class="blogger" method="post" >
	{{ form_start(form) }}
	<div class="form-group">
		{{ form_row(form.nombre) }}
	</div>

        {{ form_errors(form) }}
		
	{{ form_end(form) }}
	</form>

{% endblock %}
